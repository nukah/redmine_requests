<%= l(:mail_body_overdue, :count => @issues.size, :days => @days, :status => @status) %>:

<% @issues.each do |issue| -%>
* <%= "#{issue.project} - #{issue.tracker} ##{issue.id}: #{issue.subject}" %>
<% end -%>

<%= @issues_url %>
